from setuptools import setup, find_packages

setup(
    name="Network",              # The name of your package
    version="0.1",                # Version
    packages=find_packages(),     # Automatically include everything with __init__.py
    install_requires=[],          # List dependencies like ["numpy"]
)